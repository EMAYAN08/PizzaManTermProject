// aws-cognito/config.js

const config = {
      region: 'YOUR-REGION', // replace with your Cognito region
      userPoolId: 'YOUR-USER-POOL', // replace with your Cognito user pool id
      clientId: 'YOUR-CLIENT-ID', // replace with your Cognito client id
    };
    
export default config;
    
